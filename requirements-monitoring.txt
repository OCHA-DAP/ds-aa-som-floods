# Runtime dependencies of the daily monitoring workflow (.github/workflows/monitoring.yml).
# Kept separate from pyproject.toml because the analysis extras (geoglows -> hydrostats)
# do not build on a clean Python 3.12 runner. Install with `pip install -e . --no-deps`
# for the src package, then this file.
pandas>=2.2
numpy>=2.0
xarray>=2025.1
cfgrib>=0.9.15
eccodes==2.47.0
cdsapi>=0.7.7
matplotlib>=3.10
ocha-stratus>=0.1.7
sqlalchemy>=2.0
psycopg2-binary>=2.9
pyarrow>=17
python-dotenv>=1.0
requests>=2.32
jinja2>=3.1
ocha-relay @ git+https://github.com/OCHA-DAP/ocha-relay.git@v0.3.0
